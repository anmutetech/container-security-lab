apiVersion: v1
kind: Service
metadata:
  name: vaultpay-service
  namespace: vaultpay-ns
  labels:
    app: vaultpay
    compliance: pci-dss
spec:
  type: LoadBalancer
  selector:
    app: vaultpay
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      name: http
